{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "no-var": "off" // Disable the "no-var" rule globally
  },
  "overrides": [
    {
      "files": ["**/*.ts", "**/*.tsx"],
      "rules": {
        "no-var": "off" // Disable "no-var" specifically for TypeScript files
      }
    }
  ]
}
